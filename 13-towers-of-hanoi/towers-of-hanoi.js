'use strict';

module.exports = class TowerOfHanoi() {
  constructor(discQuantity) {
    this.stackA = [];
    this.stackB = [];
    this.stackC = [];
    this.stacks = [stackC, stackB, stackA];
  }
    
  moveDiscs() {
    while (stackA.length && stackB.length) {
    }
  }
}